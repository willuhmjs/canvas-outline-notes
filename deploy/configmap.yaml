# Combined ConfigMap for both canvas-sync and canvas-outline-notes scripts
apiVersion: v1
kind: ConfigMap
metadata:
  name: canvas-scripts
  namespace: dav
data:
  sync.py: |
    # See canvas-sync-configmap.yaml for the full script
    # This is a placeholder - copy from deploy/canvas-sync-configmap.yaml
  notes.py: |
    # See canvas-outline-configmap.yaml for the full script
    # This is a placeholder - copy from deploy/canvas-outline-configmap.yaml
